//Placeholder translation unit to smoothdemon_core has at least one symbol.
//Will be replaced with real measurement modules in Phase 1.

namespace smoothdemon {

    //Force the library to have at least one at least one emitted symbol.
    int core_placeholder() noexcept {
        return 0;
    }
}
